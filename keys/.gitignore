# Firmware-signing PRIVATE key — never commit. Generated by `make firmware-keygen`.
# The PUBLIC key is compiled into mgmtd via src/userspace/mgmtd/firmware_pubkey.h.
firmware-signing.pem
*.pem
*.key
